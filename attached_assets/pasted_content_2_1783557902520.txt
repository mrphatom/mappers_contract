You are preparing a monorepo for its first-ever push to GitHub (nothing has been  
pushed yet), followed by a 0.1.0 release. The repo is a Solana freelance-escrow  
protocol: an Anchor/Rust program (programs/project_mappers), a Node oracle service  
(oracle/, uses npm), a pnpm workspace of TS libs (lib/sdk, lib/db, lib/api-zod,  
lib/api-client-react), an Express api-server (apps/api-server), and a Vite/React  
dashboard (apps/dashboard). Do ALL of the following, in order. Do NOT push or commit  
until I review — at the end, run `git status` and show me what would be staged.  
  
=== 1. Add a root .gitignore (do this FIRST) ===  
There is currently NO .gitignore at the repo root. Create one that excludes:  
node_modules/, Anchor build output (target/, .anchor/), all dist/ build dirs  
(including oracle/dist/), all .env files (.env, .env.local, .env.*.local) but NOT  
.env.example, coverage/, log files, editor/OS junk (.DS_Store, .idea/, .vscode/),  
and any Solana keypair files (id.json, *-keypair.json, *.json keypairs). Make  
absolutely sure no secret (ORACLE_PRIVATE_KEY, DATABASE_URL, API keys, keypairs)  
can be staged.  
  
=== 2. Create committed .env.example files ===  
Placeholder (non-secret) values, one short comment per var.  
- oracle/.env.example — required: SOLANA_RPC_URL, PROGRAM_ID, ORACLE_PRIVATE_KEY,  
  HELIUS_GRPC_ENDPOINT, HELIUS_API_KEY, GEMINI_API_KEY, ANTHROPIC_API_KEY;  
  optional: GEMINI_MODEL, ANTHROPIC_MODEL, APPROVAL_CONFIDENCE_THRESHOLD,  
  REJECTION_CONFIDENCE_THRESHOLD, PORT, CORS_ALLOWED_ORIGINS, RATE_LIMIT_WINDOW_MS,  
  RATE_LIMIT_MAX_REQUESTS, ORACLE_API_KEY, SENTRY_DSN, NODE_ENV. (Source of truth:  
  oracle/src/config.ts.)  
- apps/api-server/.env.example — grep apps/api-server/src for process.env and  
  include exactly what it uses (expected: PORT, DATABASE_URL, ORACLE_URL).  
- apps/dashboard/.env.example — grep apps/dashboard/src/App.tsx and  
  apps/dashboard/vite.config.ts for import.meta.env / VITE_ and include the exact  
  VITE_ variable name(s) it reads.  
Confirm .gitignore does NOT exclude .env.example files.  
  
=== 3. Fix the Makefile / package-manager mismatch ===  
Root package.json has a preinstall that forces pnpm (deletes package-lock.json/  
yarn.lock and hard-fails on non-pnpm). The Makefile `install` target still runs  
`yarn install`, which trips the guard. Change the root `install` target to  
`pnpm install`. Leave `oracle-install` as `cd oracle && npm install` (intentional,  
matches CI). Verify `make setup` runs cleanly.  
  
=== 4. Regenerate & reconcile the IDL ===  
Three IDL copies must agree: root idl.json, oracle/idl.json, and the hand-written  
lib/sdk/src/idl.ts. lib/sdk pins @coral-xyz/anchor ^0.30 but idl.ts is in legacy  
0.29 format (isMut/isSigner, publicKey, no discriminator/metadata), which can break  
`new anchor.Program(idl, provider)` in lib/sdk/src/client.ts. Run `anchor build` to  
regenerate the canonical IDL, update all three copies to Anchor 0.30 format  
(discriminator arrays + metadata.address), confirm the program address matches  
declare_id! in programs/project_mappers/src/lib.rs  
(52yt1gCbPeiKP4JYjUVKmMJSgBMMcUx8xRGqozMKX2Mu), then remove the manual  
`{ ...IDL, address }` spread and `as any` casts in lib/sdk/src/client.ts if the  
typed IDL makes them unnecessary.  
  
=== 5. Audit & fix the dashboard (apps/dashboard) for bugs ===  
Read these files in full and fix any bugs you find (report each fix):  
- App.tsx, main.tsx, index.css  
- pages/: dashboard.tsx, jobs.tsx, create-job.tsx, job-detail.tsx, oracle.tsx,  
  not-found.tsx  
- hooks/: use-mappers-client.ts, use-mobile.tsx, use-toast.ts  
- components/: wallet-provider.tsx, wallet-button.tsx, status-badge.tsx,  
  job-card.tsx (and anything else in components/ that isn't vendored ui/ shadcn)  
- lib/: format.ts and any api/query setup  
Specifically check for: (a) any place a job's `description`/`deliverable`/`txSig` or  
other server data is rendered unsafely (dangerouslySetInnerHTML / raw HTML) — XSS  
risk; (b) missing loading/error/empty states on react-query hooks (blank screens on  
fetch failure); (c) the jobId length check — must be measured as UTF-8 BYTES  
(Buffer/TextEncoder), not string .length, to match the on-chain 32-byte cap and the  
PDA seed; (d) client-side amount validation against the rent-exempt minimum before  
building the initializeJob tx (clearer error than an on-chain failure); (e) wallet  
connection edge cases (actions available while disconnected, unhandled rejected  
signatures, no feedback on tx failure); (f) hardcoded URLs/secrets (API base URL  
should come from the VITE_ env var, not be hardcoded); (g) accessibility basics  
(buttons without labels, missing alt text, non-focusable interactive elements).  
  
=== 6. Upgrade the dashboard UI/UX (keep the existing dark glass-morphism theme) ===  
The current design uses a dark theme with glass panels, emerald/purple gradient  
accents, and monospace labels (see pages/dashboard.tsx). Improve — do not replace —  
this aesthetic:  
- Consistent design tokens: unify spacing, radii, the emerald(#14F195)/purple  
  (#9945FF) Solana accent palette, and typography scale across all pages.  
- Real loading skeletons (not spinners) for stat cards, job lists, and job detail.  
- Friendly empty states (e.g. "No jobs yet — create your first escrow") with a CTA.  
- Toast feedback on every async action (create job, submit deliverable, tx success/  
  failure) using the existing use-toast/Toaster.  
- Responsive layout: verify the sidebar/nav and grids work on mobile (there's a  
  use-mobile hook and a mobile menu in App.tsx already) — fix any overflow/cramping.  
- Clear job-status visualization (pending/completed/cancelled) with the StatusBadge,  
  and surface the oracle consensus outcome (RELEASE/REFUND/ESCALATE) on job-detail.  
- Micro-interactions: hover/focus states, smooth transitions, disabled-button states  
  during pending txs.  
- Accessibility: keyboard navigation, focus rings, aria labels, sufficient contrast.  
Keep it visually cohesive with what's there; don't introduce a new component library.  
  
=== 7. Run all CI gates locally and fix every failure (do NOT push) ===  
From repo root, in order:  
1. pnpm install  
2. pnpm run typecheck  
3. pnpm run build  
4. cd oracle && npm run test:coverage  (then cd back to root)  
5. npx prettier --check .   (if it fails, run npx prettier --write .)  
6. pnpm run test:anchor   (may need a local validator)  
Also build the dashboard specifically to catch Vite/TS errors from your UI changes.  
Report each step's output and fix anything red until all pass.  
  
=== Do NOT do yet ===  
Do not bump versions, write the CHANGELOG, or tag 0.1.0 — those happen after I review  
the pushed changes. When finished, summarize every change and every bug fixed, then  
run `git status` and show me exactly what would be staged so I can confirm no secrets  
are included.